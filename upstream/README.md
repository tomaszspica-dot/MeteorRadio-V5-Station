# Upstream MeteorRadio

The acquisition/detection foundation is:

<https://github.com/rabssm/MeteorRadio>

This repository template deliberately does not vendor the upstream source into the public tree because its redistribution/license status needs clarification.

After local import, the exact modified reference core is kept under:

```text
private_reference/upstream-meteorradio-modified/
```

That directory is ignored by Git.
