# systemd reference units

`reference/` is populated from the final installer by `IMPORT_FROM_INSTALLER.command`.

Treat those files as audited reference templates, not universal drop-in units. Adapt paths, users and the radio-ownership model for each station.
