# Assets

Add sanitized screenshots under `assets/screenshots/` following `docs/SCREENSHOTS_TO_ADD.md`.

GitHub renders the Mermaid diagrams embedded directly in the Markdown documentation, so no generated architecture image is required.
